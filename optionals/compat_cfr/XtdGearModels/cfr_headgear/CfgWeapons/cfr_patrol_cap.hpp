class acebi_mil_cap { // We append to the CUP model for sake of simplicity.
    // While labeled "Patrol Cap" in CFR, it's actually based on the vanilla "Military Cap".
    class camo
    {
        values[] += { "CFR_DUNA", "CFR_LOVEC" };
    };
};
